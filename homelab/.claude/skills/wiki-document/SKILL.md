---
name: wiki-document
description: Document a homelab component, config, or change in the wiki. Use when the human says "document the new VM", "document this OPNsense config", or similar.
---

Read `workflows/document.md` from the wiki root and follow it exactly, step by step. Do not run the procedure from memory; the workflow file is the source of truth and may have changed since you last read it. `python3 lint.py check` verifies this wrapper stays paired with its workflow.
